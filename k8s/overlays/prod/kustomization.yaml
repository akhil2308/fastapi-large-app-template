apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

images:
  - name: your-registry/fastapi-large-app-template
    newTag: latest

patches:
  - path: patches/deployment.yaml
  - path: patches/hpa.yaml
  - path: patches/configmap.yaml
