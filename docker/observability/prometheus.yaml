# Prometheus scrape configuration for observability stack.
# Scrapes metrics from the OTEL Collector, which aggregates metrics from all applications.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel-collector:9464']
